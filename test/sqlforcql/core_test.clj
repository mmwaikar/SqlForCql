(ns sqlforcql.core-test
  (:require [clojure.test :refer :all]
            [sqlforcql.core :refer :all]
            [sqlforcql.core :as core]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

